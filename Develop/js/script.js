// set present day and date

// make time slots clickable for task input and editing

// make save button and add save to localStorage

// color code status of time status event to represent past, present, or future

var rightNow = moment().format('dddd, MMMM Do YYYY');
document.getElementById("currentDay").innerText = rightNow;